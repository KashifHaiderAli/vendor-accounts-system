(function(){function n(v){return Number(v||0)||0}function recalc(row){const q=n(row.querySelector('[name="quantity"]').value),r=n(row.querySelector('[name="purchase_rate"]').value),tp=n(row.querySelector('[name="tax_percent"]').value);const base=q*r,tax=base*tp/100;row.querySelector('.pr-line-total').value=(base+tax).toFixed(2)}document.addEventListener('input',e=>{if(e.target.classList.contains('pr-calc'))recalc(e.target.closest('tr'))});document.addEventListener('click',e=>{const b=e.target.closest('.pr-remove');if(b)b.closest('tr').remove();if(e.target.closest('#pr-add-row')){const tb=document.querySelector('#purchase-return-items tbody'),tr=tb.rows[0].cloneNode(true);tr.querySelectorAll('input').forEach(i=>{if(i.type==='hidden')i.value='';else i.value=i.name==='quantity'?'1':'0';if(i.name==='description')i.value=''});tb.appendChild(tr)}});document.querySelectorAll('#purchase-return-items tbody tr').forEach(recalc)})();
